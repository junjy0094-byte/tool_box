Pillow
mss
tkinterdnd2
# Trace Mapping / Strain → CTE / Zone 분할기 의존성 (해당 도구를 열 때만 필요)
numpy
scipy
matplotlib
shapely
pcb-tools
pandas

# DB → INP 변환기 Step 1(MAPDL cleanup + CDWRITE) 전용.
# 로컬에 ANSYS 가 설치되어 있어야 하며, Step 2(CDB → INP)만 쓸 때는 필요 없다.
# ansys-mapdl-core
